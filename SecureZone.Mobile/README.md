# SecureZone Mobile

React Native / Expo presentation shell for the SecureZone mobile application.

The current implementation intentionally contains only the visual login screen. Authentication,
API calls, token storage, role routing, password recovery and navigation are outside this change.

## Local preview

```powershell
pnpm install
pnpm start
```
